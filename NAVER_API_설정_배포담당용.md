# 네이버 지도 API 배포 반영 가이드 

## 지금까지 완료된 것 
- `api/directions.js`: 네이버 API를 대신 호출해주는 서버 코드 작성 완료
- `app.js`: 네이버 실패 시 자동으로 OSRM → 추정치로 넘어가는 3단계 안전장치 구현 완료
- 네이버클라우드플랫폼에서 Directions 5 Application 등록, Client ID/Secret 발급 완료
- 이용량 상한선 설정 완료 (일 2,000건 / 월 50,000건, 무료 범위 안에서 안전하게 설정됨)
- `node test-naver.js`로 실제 API 호출 테스트 성공 확인 (거리/시간 정상 응답 받음)

**즉, 코드와 키 자체는 준비가 다 끝난 상태**이고, 남은 건 배포 환경에 반영하는 것뿐이에요.


### 1. Client ID / Secret 값 받기 (톡방에 올렸습니다.)
- `NAVER_MAPS_CLIENT_ID`
- `NAVER_MAPS_CLIENT_SECRET`

### 2. Vercel 프로젝트에 환경변수로 등록
Vercel 대시보드 → 우리 프로젝트 선택 → **Settings → Environment Variables**로 들어가서 아래 2개를 추가하세요.

| Key | Value |
|---|---|
| `NAVER_MAPS_CLIENT_ID` | 전달받은 Client ID |
| `NAVER_MAPS_CLIENT_SECRET` | 전달받은 Client Secret |

값을 코드나 GitHub에 직접 적지 마시고, **꼭 이 환경변수 등록 화면에만** 넣어주세요.

### 3. 재배포
환경변수 저장 후, Vercel에서 재배포(Redeploy)를 눌러주세요. 코드는 이미 이 값을 자동으로 읽도록 짜여 있어서, 별도 코드 수정 없이 바로 적용됩니다.

### 4. 배포된 사이트에서 확인
실제 배포 주소로 들어가서 장소 2곳 이상 선택 후 "추천 일정 만들기"를 눌러보세요.
- 결과 화면 상단 안내문구가 **"네이버 지도의 실시간 교통 정보를 반영한..."**으로 뜨면 성공
- 만약 **"길찾기 서버 응답이 없어 추정치로 계산했어요"**로 뜨면 환경변수가 제대로 등록 안 됐거나 오타가 있는 거예요 → 1~2번 다시 확인

## 알아두시면 좋은 것
- 이용량 상한선을 넘거나 네이버 서버에 문제가 생겨도, 서비스가 멈추지 않고 자동으로 OSRM(무료 대체 경로 계산)으로 전환돼요. 즉 이 설정 과정에서 실수해도 사이트 자체가 죽을 위험은 없어요.
- `test-naver.js`는 로컬 개발용 테스트 파일이라 배포에는 영향 없어요. 그냥 두셔도 되고, 지우셔도 상관없어요.
